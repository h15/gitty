% extends 'base';

% content_for header => begin
    <%=l 'Model\'s list' %>
% end

% content body => begin

<div>
% for my $m ( keys %$models ) {
    <article class="plugin rounded">
        <a href="<%= url_for('mojo_m_read', id => $m) %>">
%= $m
        </a>
    </article>
% }
</div>

% end
